# Optional locally: long random string so sessions survive app restarts (required on Render).
# SECRET_KEY=change-me

# Render + persistent disk: mount disk (e.g. at /data) and point SQLite here.
# DATABASE_PATH=/data/student_desk.db
